redis:
    # redis数据库索引(默认为0)，我们使用索引为3的数据库，避免和其他数据库冲突
    database: 3
    # redis服务器地址（默认为loaclhost）
    host: 192.168.73.12
    # redis端口（默认为6379）
    post: 6379
    # redis访问密码（默认为空）
    password: pwd123
    # redis连接超时时间（单位毫秒）
    timeout: 0
